# Copy this file to .env.local and set the value for your environment.
# .env.local is gitignored. .env.example IS committed so collaborators
# know which variables to set without having to read the source code.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
